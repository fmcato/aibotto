# LLM Providers Configuration
# ============================
# Define shared LLM API providers here.
# Environment variables are supported with ${VAR_NAME} or ${VAR_NAME:-default} syntax.

providers:
  default:
    api_key_env: OPENAI_API_KEY
    base_url: ${OPENAI_BASE_URL:-https://api.openai.com/v1}
